{
  "name": "cat-care-backend",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "seed": "npx ts-node -r tsconfig-paths/register src/seeds/seed.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.5.0"
  }
}
